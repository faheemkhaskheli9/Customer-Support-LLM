# LLM Fundamentals

An LLM predicts tokens from context. It can produce useful language without possessing a verified database of facts. Quality depends on model, prompt, context, decoding settings, and surrounding controls.

Track context length, input and output tokens, latency, error rates, and cost. Treat model output as untrusted data until it passes schema, policy, and evidence checks.

# Deployment

Deploy the assistant as a bounded service with secret management, request limits, timeouts, dependency health checks, structured logs, traces, and redacted metrics.

Release prompts, models, retrievers, policies, and corpora as versioned artifacts. Use canaries, regression gates, rollback, and a kill switch. Define retention and deletion behavior before collecting production conversations.

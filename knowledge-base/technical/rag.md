# Retrieval-Augmented Generation

RAG has two independent quality problems: finding the right evidence and generating only from that evidence. Tune chunking, query rewriting, filters, top-k, and reranking against a labeled retrieval set.

On an empty or low-confidence result, return a deterministic limitation. Preserve source title, URL or document ID, revision, and chunk metadata for citations and audits.

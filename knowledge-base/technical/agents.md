# Agents

An agent is a model-driven loop that selects actions, observes results, and continues toward a goal. Use one only when fixed orchestration cannot express the workflow clearly.

Bound iterations, elapsed time, tokens, tools, and spend. Model every terminal and recoverable state. Prefer deterministic workflows for high-risk operations and require confirmation before side effects.

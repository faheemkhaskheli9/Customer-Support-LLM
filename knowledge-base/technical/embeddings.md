# Embeddings

An embedding maps text to a vector so semantically related items can be compared. Choose a model appropriate to language, domain, privacy boundary, and cost.

Record embedding model and preprocessing version with every vector. Re-embed when either changes. Evaluate nearest-neighbor quality on representative support queries instead of assuming cosine similarity equals relevance.

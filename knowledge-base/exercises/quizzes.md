# Quizzes

Use short scenario questions after each module. Favor diagnosis of failure modes over vocabulary recall.

Sample questions:

- What should happen when retrieval returns no chunks?
- Why must tool authorization live outside the model?
- Which metrics distinguish a retriever regression from a generation regression?
- When should a support interaction be handed to a human?
